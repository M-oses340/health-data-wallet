{
  "name": "health-data-monetization-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "dependencies": {
    "@noble/secp256k1": "^2.1.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
